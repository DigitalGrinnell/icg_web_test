---
target:
  site-description: 'Simple IP Test'                  # this name is reported as a header in the output
  base-url: 'https://172.217.9.46'                    # this is the base URL for all tests

  tests:

    - description: 'Google.com'
      url: '/'
      delay: 5
      match:
        - id: 'hplogo'
